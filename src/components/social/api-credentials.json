{
  "collectionName": "components_social_api_credentials",
  "info": {
    "displayName": "API Credentials",
    "description": "API credentials for social media platforms (encrypted)"
  },
  "options": {},
  "attributes": {
    "api_key": {
      "type": "string",
      "description": "API key (encrypted)"
    },
    "api_secret": {
      "type": "string",
      "description": "API secret (encrypted)"
    },
    "access_token": {
      "type": "string",
      "description": "Access token (encrypted)"
    },
    "refresh_token": {
      "type": "string",
      "description": "Refresh token (encrypted)"
    },
    "expires_at": {
      "type": "datetime",
      "description": "Token expiration date"
    },
    "is_valid": {
      "type": "boolean",
      "default": false,
      "description": "Whether credentials are valid"
    },
    "last_verified": {
      "type": "datetime",
      "description": "Last time credentials were verified"
    }
  }
}
